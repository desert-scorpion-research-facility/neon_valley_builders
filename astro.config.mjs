// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://neonvalleybuilders.com',
    // No base needed for custom domain
    output: 'static',
});
